# Copy this file to .env and adjust as needed.
# Every variable has a sensible default, so the app also runs with no .env at all.

# --- HTTP server ---
PORT=4000
NODE_ENV=development

# Origin allowed by CORS during development (the Vite dev server).
CLIENT_ORIGIN=http://localhost:5173

# --- Email service ---
# "console" logs the message to stdout (default, no credentials required).
# "smtp" is a placeholder for a real transport (see server/src/services/email.service.ts).
EMAIL_TRANSPORT=console

# Inbox that receives the contact notifications.
CONTACT_TO=contacto.mfsoluciones@gmail.com
CONTACT_FROM=contacto.mfsoluciones@gmail.com

# --- Contact rate limiting ---
CONTACT_RATE_LIMIT_MAX=5
CONTACT_RATE_LIMIT_WINDOW_MS=3600000
